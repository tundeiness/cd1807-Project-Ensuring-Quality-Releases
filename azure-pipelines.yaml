name: Azure Pipelines

trigger:
  - main

pool:
  name: myAgentPool

variables:
  - group: azure-credentials
  - name: python.version
    value: '3.8.10'
  - name: azureServiceConnectionId
    value: 'myConnex'
  - name: projectRoot
    value: $(System.DefaultWorkingDirectory)
  - name: environmentName
    value: 'test'
  - name: webAppName
    value: 'Udacity-app-3bb41105'

stages:
  #--------------------------------------------#
  # BUILD STAGE
  #--------------------------------------------#
  - stage: Build
    displayName: Build Stage
    jobs:
      - job: BuildInfrastructure
        displayName: Terraform | Provision / Refresh Infra
        steps:
          - task: ms-devlabs.custom-terraform-tasks.custom-terraform-installer-task.TerraformInstaller@0
            displayName: 'Terraform installation'
            inputs:
              terraformVersion: '1.2.9'

          - task: ms-devlabs.custom-terraform-tasks.custom-terraform-release-task.TerraformTaskV3@3
            displayName: 'Terraform init'
            inputs:
              provider: 'azurerm'
              command: 'init'
              workingDirectory: '$(System.DefaultWorkingDirectory)/terraform/environments/test'
              backendServiceArm: '$(azureServiceConnectionId)'
              backendAzureRmResourceGroupName: 'Azuredevops'
              backendAzureRmStorageAccountName: 'tfstate182032827'
              backendAzureRmContainerName: 'tfstate'
              backendAzureRmKey: 'test.terraform.tfstate'
            env:
              ARM_CLIENT_ID: $(ARM_CLIENT_ID)
              ARM_CLIENT_SECRET: $(ARM_CLIENT_SECRET)
              ARM_TENANT_ID: $(ARM_TENANT_ID)
              ARM_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)

          - task: DownloadSecureFile@1
            name: sshKey
            displayName: 'Download SSH Private Key'
            inputs:
              secureFile: 'id_rsa'

          - task: DownloadSecureFile@1
            name: sshPubKey
            displayName: 'Download SSH Public Key'
            inputs:
              secureFile: 'id_rsa.pub'

          - task: InstallSSHKey@0
            displayName: 'Install SSH Key for VM Access'
            inputs:
              knownHostsEntry: '$(KNOWN_HOSTS_STRING)'
              sshPublicKey: '$(SSH_PUBLIC_KEY)'
              sshKeySecureFile: 'id_rsa'

          - script: |
              cd $(System.DefaultWorkingDirectory)/terraform/environments/test
              terraform validate
            displayName: 'Terraform validate'
            env:
              ARM_CLIENT_ID: $(ARM_CLIENT_ID)
              ARM_CLIENT_SECRET: $(ARM_CLIENT_SECRET)
              ARM_TENANT_ID: $(ARM_TENANT_ID)
              ARM_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)
              TF_VAR_ssh_public_key: $(cat $(sshPubKey.secureFilePath))

          - script: |
              export ARM_CLIENT_ID=$(ARM_CLIENT_ID)
              export ARM_CLIENT_SECRET=$(ARM_CLIENT_SECRET)
              export ARM_TENANT_ID=$(ARM_TENANT_ID)
              export ARM_SUBSCRIPTION_ID=$(ARM_SUBSCRIPTION_ID)
              cd $(System.DefaultWorkingDirectory)/terraform/environments/test
              SSH_KEY=$(cat $(sshPubKey.secureFilePath))
              terraform plan -detailed-exitcode -var="ssh_public_key=$SSH_KEY"
              exit_code=$?
              if [ $exit_code -eq 1 ]; then
                echo "##[error]Terraform plan failed"
                exit 1
              fi
              echo "Terraform plan succeeded (exit code: $exit_code)"
              exit 0
            displayName: 'Terraform plan'
            env:
              ARM_CLIENT_ID: $(ARM_CLIENT_ID)
              ARM_CLIENT_SECRET: $(ARM_CLIENT_SECRET)
              ARM_TENANT_ID: $(ARM_TENANT_ID)
              ARM_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)

          - script: |
              export ARM_CLIENT_ID=$(ARM_CLIENT_ID)
              export ARM_CLIENT_SECRET=$(ARM_CLIENT_SECRET)
              export ARM_TENANT_ID=$(ARM_TENANT_ID)
              export ARM_SUBSCRIPTION_ID=$(ARM_SUBSCRIPTION_ID)
              cd $(System.DefaultWorkingDirectory)/terraform/environments/test
              SSH_KEY=$(cat $(sshPubKey.secureFilePath))
              terraform apply -auto-approve -var="ssh_public_key=$SSH_KEY"
            displayName: 'Terraform apply'
            env:
              ARM_CLIENT_ID: $(ARM_CLIENT_ID)
              ARM_CLIENT_SECRET: $(ARM_CLIENT_SECRET)
              ARM_TENANT_ID: $(ARM_TENANT_ID)
              ARM_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)

      - job: ArchiveArtifacts
        displayName: Archive | Publish Build Artifacts
        dependsOn: BuildInfrastructure
        steps:
          - task: ArchiveFiles@2
            displayName: 'Archive Selenium UI Tests'
            inputs:
              rootFolderOrFile: '$(System.DefaultWorkingDirectory)/automatedtesting/selenium'
              includeRootFolder: false
              archiveType: 'zip'
              archiveFile: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId)-uitests.zip'
              replaceExistingArchive: true

          - publish: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId)-uitests.zip'
            displayName: 'Publish Selenium UI Tests Package'
            artifact: drop-uitests

          - task: ArchiveFiles@2
            displayName: 'Archive FakeRestAPI'
            inputs:
              rootFolderOrFile: '$(System.DefaultWorkingDirectory)/automatedtesting/jmeter/fakerestapi'
              includeRootFolder: false
              archiveType: 'zip'
              archiveFile: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId)-fakerestapi.zip'
              replaceExistingArchive: true

          - publish: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId)-fakerestapi.zip'
            displayName: 'Publish FakeRestAPI Package'
            artifact: drop-fakerestapi

          - task: ArchiveFiles@2
            displayName: 'Archive JMeter Performance Tests'
            inputs:
              rootFolderOrFile: '$(System.DefaultWorkingDirectory)/automatedtesting/jmeter/test-suite'
              includeRootFolder: false
              archiveType: 'zip'
              archiveFile: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId)-perftests.zip'
              replaceExistingArchive: true

          - publish: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId)-perftests.zip'
            displayName: 'Publish JMeter Performance Tests Package'
            artifact: drop-perftests

  #--------------------------------------------#
  # DEPLOYMENT STAGE
  #--------------------------------------------#
  - stage: Deploy
    displayName: Deploy Stage
    dependsOn: Build
    jobs:
      - deployment: DeployFakeRestAPI
        displayName: Deploy | FakeRestAPI to Web App
        environment: $(environmentName)
        strategy:
          runOnce:
            deploy:
              steps:
                - download: current
                  artifact: drop-fakerestapi
                  displayName: 'Download FakeRestAPI Artifact'

                - task: AzureWebApp@1
                  displayName: 'Deploy FakeRestAPI to Azure Web App'
                  inputs:
                    azureSubscription: '$(azureServiceConnectionId)'
                    appName: '$(webAppName)'
                    appType: 'webApp'
                    package: '$(Pipeline.Workspace)/drop-fakerestapi/$(Build.BuildId)-fakerestapi.zip'
                    deploymentMethod: 'zipDeploy'

      - job: PostmanTests
        displayName: Postman | API Tests
        dependsOn: DeployFakeRestAPI
        steps:
          - task: NodeTool@0
            displayName: 'Install Node.js'
            inputs:
              versionSpec: '16.x'

          - script: |
              npm install -g newman
            displayName: 'Install Newman'

          - script: |
              set -e
              cd "$(System.DefaultWorkingDirectory)/automatedtesting/postman"

              newman run TestSuite.Regression.json \
                --env-var "baseurl=udacity-app-3bb41105.azurewebsites.net" \
                --env-var "protocol=https" \
                --reporters cli,junit \
                --reporter-junit-export result-Regression.xml
            displayName: 'Run Regression Tests'

          - script: |
              set -e
              cd "$(System.DefaultWorkingDirectory)/automatedtesting/postman"

              newman run TestSuite.Data-Validation.json \
                --env-var "baseurl=udacity-app-3bb41105.azurewebsites.net" \
                --env-var "protocol=https" \
                --reporters cli,junit \
                --reporter-junit-export result-DataValidation.xml
            displayName: 'Run Data Validation Tests'

          - task: PublishTestResults@2
            displayName: 'Publish Postman Regression Results'
            inputs:
              testResultsFormat: 'JUnit'
              testResultsFiles: 'result-Regression.xml'
              searchFolder: '$(System.DefaultWorkingDirectory)/automatedtesting/postman'
              failTaskOnFailedTests: false
              testRunTitle: 'API Regression Tests'
              publishRunAttachments: true

          - task: PublishTestResults@2
            displayName: 'Publish Postman Data Validation Results'
            inputs:
              testResultsFormat: 'JUnit'
              testResultsFiles: 'result-DataValidation.xml'
              searchFolder: '$(System.DefaultWorkingDirectory)/automatedtesting/postman'
              failTaskOnFailedTests: false
              testRunTitle: 'API Data Validation Tests'
              publishRunAttachments: true

      - deployment: RunJMeterTests
        displayName: Test | JMeter Performance
        dependsOn: DeployFakeRestAPI
        pool:
          name: myAgentPool
        environment: $(environmentName)
        strategy:
          runOnce:
            deploy:
              steps:
                - download: current
                  artifact: drop-perftests
                  displayName: 'Download JMeter Test Suite'

                - task: Bash@3
                  displayName: 'Install Java + Apache JMeter'
                  inputs:
                    targetType: 'inline'
                    script: |
                      set -euo pipefail

                      sudo apt-get update -y
                      sudo apt-get install -y openjdk-11-jre-headless unzip tar wget

                      export JAVA_HOME="$(dirname "$(dirname "$(readlink -f "$(command -v java)")")")"
                      export PATH="$JAVA_HOME/bin:$PATH"
                      java -version

                      TOOLS_DIR="$(Pipeline.Workspace)/tools"
                      JMETER_VERSION="5.6.3"
                      JMETER_DIR="$TOOLS_DIR/apache-jmeter-$JMETER_VERSION"
                      JMETER_BIN="$JMETER_DIR/bin/jmeter"

                      mkdir -p "$TOOLS_DIR"
                      cd "$TOOLS_DIR"

                      if [[ ! -d "apache-jmeter-$JMETER_VERSION" ]]; then
                        wget -q "https://archive.apache.org/dist/jmeter/binaries/apache-jmeter-$JMETER_VERSION.tgz"
                        tar -xzf "apache-jmeter-$JMETER_VERSION.tgz"
                      fi

                      "$JMETER_BIN" -v

                - task: Bash@3
                  displayName: 'Run JMeter Stress Test Suite'
                  inputs:
                    targetType: 'inline'
                    script: |
                      set -euo pipefail

                      export JAVA_HOME="$(dirname "$(dirname "$(readlink -f "$(command -v java)")")")"
                      export PATH="$JAVA_HOME/bin:$PATH"

                      cd "$(Pipeline.Workspace)/drop-perftests"

                      TOOLS_DIR="$(Pipeline.Workspace)/tools"
                      JMETER_VERSION="5.6.3"
                      JMETER="$TOOLS_DIR/apache-jmeter-$JMETER_VERSION/bin/jmeter"

                      ZIP_FILE="$(ls -1 *.zip 2>/dev/null | head -n 1 || true)"
                      if [[ -z "${ZIP_FILE}" ]]; then
                        echo "No .zip found in $(pwd)"
                        ls -la
                        exit 1
                      fi

                      rm -rf perftests
                      mkdir -p perftests
                      unzip -o "$ZIP_FILE" -d perftests
                      ls -la perftests

                      OUT_DIR="$(Build.ArtifactStagingDirectory)/jmeter/stress"
                      JTL_FILE="$OUT_DIR/stress-test-results.jtl"
                      LOG_FILE="$OUT_DIR/stress-test.log"
                      REPORT_DIR="$OUT_DIR/stress-test-report"

                      rm -rf "$OUT_DIR"
                      mkdir -p "$OUT_DIR"
                      rm -rf "$REPORT_DIR"

                      set +e
                      "$JMETER" -n \
                        -t perftests/StressTestSuite.jmx \
                        -JAPPSERVICEURL="https://$(webAppName).azurewebsites.net" \
                        -l "$JTL_FILE" \
                        -j "$LOG_FILE" \
                        -e -o "$REPORT_DIR"
                      EXIT_CODE=$?
                      set -e

                      if [[ "$EXIT_CODE" -ne 0 ]]; then
                        echo "JMeter Stress test failed with exit code $EXIT_CODE"
                        echo "=== Last 200 lines of JMeter log ==="
                        if [[ -f "$LOG_FILE" ]]; then
                          tail -n 200 "$LOG_FILE"
                        else
                          echo "No log file found: $LOG_FILE"
                        fi
                        exit "$EXIT_CODE"
                      fi

                - task: Bash@3
                  displayName: 'Run JMeter Endurance Test Suite'
                  inputs:
                    targetType: 'inline'
                    script: |
                      set -euo pipefail

                      export JAVA_HOME="$(dirname "$(dirname "$(readlink -f "$(command -v java)")")")"
                      export PATH="$JAVA_HOME/bin:$PATH"

                      cd "$(Pipeline.Workspace)/drop-perftests"

                      TOOLS_DIR="$(Pipeline.Workspace)/tools"
                      JMETER_VERSION="5.6.3"
                      JMETER="$TOOLS_DIR/apache-jmeter-$JMETER_VERSION/bin/jmeter"

                      ZIP_FILE="$(ls -1 *.zip 2>/dev/null | head -n 1 || true)"
                      if [[ -z "${ZIP_FILE}" ]]; then
                        echo "No .zip found in $(pwd)"
                        ls -la
                        exit 1
                      fi

                      rm -rf perftests
                      mkdir -p perftests
                      unzip -o "$ZIP_FILE" -d perftests
                      ls -la perftests

                      OUT_DIR="$(Build.ArtifactStagingDirectory)/jmeter/endurance"
                      JTL_FILE="$OUT_DIR/endurance-test-results.jtl"
                      LOG_FILE="$OUT_DIR/endurance-test.log"
                      REPORT_DIR="$OUT_DIR/endurance-test-report"

                      rm -rf "$OUT_DIR"
                      mkdir -p "$OUT_DIR"
                      rm -rf "$REPORT_DIR"

                      set +e
                      "$JMETER" -n \
                        -t perftests/EnduranceTestSuite.jmx \
                        -JAPPSERVICEURL="https://$(webAppName).azurewebsites.net" \
                        -l "$JTL_FILE" \
                        -j "$LOG_FILE" \
                        -e -o "$REPORT_DIR"
                      EXIT_CODE=$?
                      set -e

                      if [[ "$EXIT_CODE" -ne 0 ]]; then
                        echo "JMeter Endurance test failed with exit code $EXIT_CODE"
                        echo "=== Last 200 lines of JMeter log ==="
                        if [[ -f "$LOG_FILE" ]]; then
                          tail -n 200 "$LOG_FILE"
                        else
                          echo "No log file found: $LOG_FILE"
                        fi
                        exit "$EXIT_CODE"
                      fi

                - publish: $(Build.ArtifactStagingDirectory)
                  displayName: 'Publish JMeter Reports, Logs, and JTL Results'
                  artifact: jmeter-reports
